# ==================================================
# DigiHub - Environment Configuration Template
# ==================================================
# 
# This is a template file. DO NOT put real credentials here!
# 
# For Local Development:
#   - Copy this to .env.local and use local settings
#   - Run: npm run db:switch local
#
# For Remote VPS:
#   - Copy this to .env.remote and use remote settings  
#   - Run: npm run db:switch remote
#
# ==================================================

# Database Configuration
# For Local: DB_HOST=localhost
# For Remote: DB_HOST=your.vps.ip.address
DB_HOST=localhost
DB_PORT=5432
DB_NAME=digihub
DB_USER=your_db_user
DB_PASSWORD=your_secure_password_here

# JWT Authentication
JWT_SECRET=change-this-to-a-random-secret-key-in-production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development
