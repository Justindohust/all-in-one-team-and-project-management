# ==================================================
# DigiHub - Environment Configuration Template
# ==================================================
# 
# Copy this file to:
# - env.local (for Docker local development)
# - env.remote (for VPS production)
#
# Then run: npm run db:switch <local|remote>
# ==================================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=digihub
DB_USER=your_db_user
DB_PASSWORD=your_secure_password_here

# JWT Authentication
JWT_SECRET=change-this-to-a-random-secret-key-in-production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development
