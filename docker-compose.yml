version: '3.8'

services:
  digihub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digihub-app
    ports:
      - "3000:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
